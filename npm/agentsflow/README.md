# agentsflow

CLI for reusable agent workflows across agent coding tools.

```sh
npm install -g agentsflow
agentsflow --version
```

The npm package installs a small Node.js launcher and a platform-specific
prebuilt agentsflow binary via optional dependencies.
