version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused

formatters:
  enable:
    - goimports

  settings:
    goimports:
      local-prefixes:
        - github.com/cairon666/agentsflow
